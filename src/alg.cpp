uint64_t nPrime(uint64_t n) {
  int znach(int *a,int n){
    for int i=0; i<n: i++)
      if(n==1 || a[i]==2 || a[i]==3 || a[i]==5 || a[i]==7)
        return true;
    else if(a[i]%2!=0 && a[i]%3!=0 && a[i]%5!=0 && a[i]%7!=0)
      return a[i];
  }
}

uint64_t nextPrime(uint64_t value) {
  int znach(int n){
    int count=0
      if(count!=0)
        if(n==1 || a[i]==2 || a[i]==3 || a[i]==5 || a[i]==7)
          return n;
        else if(n%2!=0 && n%3!=0 && n%5!=0 && n%7!=0)
          return n;
    count+=1;
  }
}

uint64_t sumPrime(uint64_t hbound) {
  int znach(int n){
    int sum=0;
    if(n==1 || a[i]==2 || a[i]==3 || a[i]==5 || a[i]==7)
      sum+=n;
      return n+1;
    else if(n%2!=0 && n%3!=0 && n%5!=0 && n%7!=0)
      sum+=n;
    return n+1;
    else
      return n+1;
  }
}
